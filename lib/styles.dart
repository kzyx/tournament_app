/// This file contains classes and/or functions relating to some default
/// and/or preset color, font, and text styles
import 'package:flutter/material.dart';

// Default colors
const primaryColor = Colors.teal;
const secondaryColor = Colors.orange;

// Default font
const defaultFont = "Urbanist";

// Some default text styles
const TextStyle whiteTextStyle =
    TextStyle(fontSize: 17, fontWeight: FontWeight.bold, color: Colors.white);
const TextStyle blackTextStyle =
    TextStyle(fontSize: 17, fontWeight: FontWeight.bold, color: Colors.black);